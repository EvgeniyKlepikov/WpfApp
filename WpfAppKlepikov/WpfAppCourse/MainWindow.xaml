<Window x:Class="WpfAppCourse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:WpfAppCourse.ViewModels"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppCourse"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="714">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5,5,5,5"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="DarkGoldenrod"/>
            <Setter Property="BorderBrush" Value="Olive"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="DarkGoldenrod"/>
            <Setter Property="BorderBrush" Value="Black"/>
        </Style>
    </Window.Resources>    
    
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <!--<ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>-->
        </Grid.ColumnDefinitions>
        <DockPanel >
            <GroupBox Header="Список автомобилей" Margin="2" Padding="2" MinWidth="200">
                <ListView x:Name="Cars" ItemsSource="{Binding Cars}"
                        SelectedValue="{Binding SelectedCar}" SelectedIndex="0">
                    <!--#region Обработка события Selection Canged -->
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding GetApplicationsCommand}"
                                                   CommandParameter="{Binding SelectedCar.CarId}">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <!--#endregion-->
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CarName}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox >
            <GroupBox Header="Параметры автомобиля" Margin="2" Padding="2">
                <!--#region Таблица -->
                <Grid DataContext="{Binding SelectedCar}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto" ></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock HorizontalAlignment="Right">Наименование автомобиля:</TextBlock>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CarName}" Background="LightGray"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right">Гос. Номер:</TextBlock>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CarNumber}" Background="LightGray"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right">Грузоподъемность:</TextBlock>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CarWeight}" Background="LightGray"/>
                </Grid>
                <!--#endregion-->
            </GroupBox>
        </DockPanel>
        <DockPanel Grid.Row="1">
            <GroupBox Header="Список заявок" Margin="2" Padding="2">
                <ListView x:Name="Applications" ItemsSource="{Binding Applications}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CargoName}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>
        </DockPanel>
    </Grid>
</Window>
